import React from 'react';
import Admin from '.';

const AdminUserManager = () => {
    return (
        <div>
            This is admin usermanager
        </div>
    );
};

export default AdminUserManager;